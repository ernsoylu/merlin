# Exact pins. Determinism (PROJECT_DEFINITION.md 3.3) depends on the template
# engine version in particular -- a jinja2 bump can change whitespace handling
# and therefore generated bytes. Bump deliberately, regenerate fixtures, review
# the diff.
jinja2==3.1.4
jsonschema==4.23.0
questionary==2.0.1
pytest==8.3.3
